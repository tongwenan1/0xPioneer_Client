export enum MapBuildingType {
    resource = 3,
    explore = 1,
    stronghold = 2,
    city = 0,
    event = 4,
    decorate = 5
}

export enum BuildingFactionType {
    netural = 0,
    self = 1,
    enemy = 2,
}

export enum BuildingStayPosType {
    One = 0,
    Three = 1,
    Seven = 2
}